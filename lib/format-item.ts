// import type { SteamMarketItem, FormattedItem } from "@/types/steam";

// export const formatItem = (item: SteamMarketItem): FormattedItem => ({
//       id: item.hash_name || '',
//       name: item.name || 'Unknown',
//       price: item.sell_price || 0,
//       image: `https://steamcommunity-a.akamaihd.net/economy/image/${item.asset_description?.icon_url || ''}`,
//       volume: item.sell_listings
// });
